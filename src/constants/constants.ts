export enum ActionConstant {
    SET_USERS = 'SET_USERS',
    SET_EMAIL_TEMPLETE = 'SET_EMAIL_TEMPLETE'
}

export enum UserType { 
    nonprofit = 'non-profit-user',
    foundation = 'foundation-user'
}